<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test2">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test2">
        <activity android:name=".OrderDetail"></activity>
        <activity android:name=".OrderQuery" />
        <activity android:name=".ExhibitionManage" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Exhibition_2D" />
        <activity android:name=".ExhibitionEdit" />
        <activity android:name=".Login" />
        <activity android:name=".CommodityDetail" />
        <activity android:name=".activity.Commodity" />
        <activity android:name=".Exhibition" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>